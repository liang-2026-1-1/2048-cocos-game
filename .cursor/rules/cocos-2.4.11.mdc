---
alwaysApply: true
---
# Cocos Creator 2.4.11 项目开发规则

本项目是基于 **Cocos Creator 2.4.11** 引擎开发的 2048 游戏（IAA游戏）。
设计分辨率: 基于720×1560，固定宽度策略
参考文档 (Context)
API 文档: https://docs.cocos.com/creator/2.4/api/zh/
用户手册: https://docs.cocos.com/creator/2.4/manual/zh/
(注：遇到不确定的 API 时，请优先参考上述 2.4 版本文档，而非通用知识库)

## 代码规范

### 事件注册
- 注册事件时优先使用基类的 `onClick` 方法，而不是直接使用 `node.on`
- 避免在 `onClick` 中使用箭头函数，应使用具体的方法名 + `.bind(this)`

```typescript
// 推荐
this.onClick(this.ui.button, this.onButtonClick.bind(this));

// 不推荐
this.onClick(this.ui.button, () => this.doSomething());
```
